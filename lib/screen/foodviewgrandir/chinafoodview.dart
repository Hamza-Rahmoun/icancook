import 'package:flutter/material.dart';

class ChinaFoodView extends StatelessWidget {
  static const routeName = 'cfv';

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text('china')),
    );
  }
}
